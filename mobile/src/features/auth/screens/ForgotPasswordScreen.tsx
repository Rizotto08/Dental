import React from 'react';
import { Text } from 'react-native';
import { Screen } from '@/components/ui/Screen';

export const ForgotPasswordScreen = () => (
  <Screen>
    <Text>Forgot password + email verification flow</Text>
  </Screen>
);
