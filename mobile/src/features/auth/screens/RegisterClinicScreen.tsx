import React from 'react';
import { Text } from 'react-native';
import { Screen } from '@/components/ui/Screen';

export const RegisterClinicScreen = () => (
  <Screen>
    <Text>Clinic registration + admin creation (7-day trial)</Text>
  </Screen>
);
