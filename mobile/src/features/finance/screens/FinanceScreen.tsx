import React from 'react';
import { Text } from 'react-native';
import { Screen } from '@/components/ui/Screen';

export const FinanceScreen = () => (
  <Screen>
    <Text>Revenue analytics, expenses, CSV export, debts</Text>
  </Screen>
);
